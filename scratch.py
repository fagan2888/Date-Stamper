
        #
        # apply font:
        # font = applyFont(font, pct, color, transparency)
        #font = applyFont(self.font, self.fontsize, self.dateColor)
        #
        # calculate position
        # pos = calcPosition(tmp.Size, placement, xOffset, yOffset, text.getsize)

        #bmp = write(self.dateText, tmp, pos=pos, font=font, color=color)
        #print tmp.GetSize()

        #color = self.dateColor

                #try
        #    del frame.imbmp
        #except:
        #    pass

        #frame.imbmp= wx.StaticBitmap(frame.imPanel,-1,write(self.dateText,tmp))

                #help(imbmp.SetBitmap)

        #frame.imbmp.Center()